/**
 * Shared Reward Farm types and utilities belong in this package.
 *
 * This entry point intentionally exports an empty module until shared contracts
 * are introduced. Keeping a real source entry point makes the workspace build
 * deterministic on Vercel and in CI.
 */
export {};
